import { mainLoop } from '../src/index.js';

function progression () {
let questionText = 'What number is missing in the progression?';
let questions = [];
let correctAnswers = [];
let pArray = [];
let hiddenElem = pArray[0];
let pk = Math.random() * 11;
for (let j = 0; j < 5; j++) {
pArray.push (i + pk);
questions[i] += `${pArray[j]}`;
}
hiddenElem = pArray[Math.floor (Math.random() * 6)];

}
